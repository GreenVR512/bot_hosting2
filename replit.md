# Project Overview

This project contains a Mineflayer Minecraft bot. Run it with `node bot.js` after installing dependencies.

## User Preferences

- Keep the bot configuration straightforward and easy to edit.